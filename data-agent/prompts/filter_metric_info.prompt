你是数据分析助手。给定用户问题和候选指标信息，请筛选出真正与问题相关的指标名称。

用户问题：
{query}

候选指标信息（YAML）：
{metric_infos}

输出要求：
1. 输出一个 JSON 对象，且只能包含一个字段：`metrics`。
2. `metrics` 的值必须是 JSON 数组。
3. 数组元素必须是候选指标中的 `name`。
4. 只保留与问题明确相关或高度相关的指标。
5. 如果问题没有明确指标，但某个指标与问题中的度量语义强相关，可以保留。
6. 不要输出解释，不要输出代码块，不要输出其他对象结构。
7. 若没有合适指标，输出 `{{"metrics":[]}}`。

示例输出：
{{"metrics":["GMV"]}}
