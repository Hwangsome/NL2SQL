你是资深数据分析工程师。请根据用户问题、候选表字段、候选指标、日期信息和数据库信息，生成一条可在目标数据库执行的 SQL。

用户问题：
{query}

候选表字段（YAML）：
{table_infos}

候选指标（YAML）：
{metric_infos}

日期信息（YAML）：
{date_info}

数据库信息（YAML）：
{db_info}

生成要求：
1. 只输出一条 SQL，不要输出解释、Markdown、代码块。
2. 只能使用候选表和候选字段。
3. SQL 必须是只读查询，使用 `SELECT` 开头。
4. 若需要关联，优先按同名主键/外键关联，典型关系包括：
   - fact_order.customer_id = dim_customer.customer_id
   - fact_order.product_id = dim_product.product_id
   - fact_order.date_id = dim_date.date_id
   - fact_order.region_id = dim_region.region_id
5. 时间语义需要结合日期信息推断：
   - “今年”必须使用 `date_info.year`
   - “去年”必须使用 `date_info.last_year`
   - “本季度”必须使用 `date_info.current_quarter`
   - 如果用户问题中已经带有“时间解析：...”，必须严格按解析后的具体时间值生成 SQL，不能自行改写年份
6. 常见指标口径：
   - GMV = SUM(fact_order.order_amount)
   - 销量 = SUM(fact_order.order_quantity)
   - AOV = AVG(fact_order.order_amount)
7. 如果问题要求“各地区 / 各品类 / 各品牌 / 各会员等级”等分组，必须使用对应维度字段 `GROUP BY`。
8. 如果问题要求排序，优先按核心指标降序。
9. 尽量生成简洁 SQL，不要使用未提供信息无法证明正确的复杂逻辑。
10. 字段和表名保持原始英文名，不要翻译。

输出示例：
SELECT r.region_name, SUM(f.order_amount) AS gmv
FROM fact_order f
JOIN dim_region r ON f.region_id = r.region_id
JOIN dim_date d ON f.date_id = d.date_id
WHERE d.year = 2024
GROUP BY r.region_name
ORDER BY gmv DESC
